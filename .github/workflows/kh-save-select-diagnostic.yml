name: Build KH H control-flow diagnostic

on:
  workflow_dispatch:
  push:
    branches:
      - fix/kh-save-select-black-screen

jobs:
  build:
    runs-on: ubuntu-latest
    container: devkitpro/devkitarm:20241104
    name: H-control-flow-vblank-arm
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Build diagnostic NDS
        shell: bash
        run: >-
          make -C code
          GBAR3_CONTROL_FLOW_DIAGNOSTICS=1
      - name: Test diagnostic decoder
        shell: bash
        run: python3 tools/diagnostics/test_decode_g3cf.py
      - name: Prepare hardware-test artifact
        shell: bash
        run: |
          mkdir -p "out/H-control-flow-vblank-arm/_gba"
          cp code/bootstrap/GBARunner3.nds "out/H-control-flow-vblank-arm/GBARunner3-KH-H-control-flow-vblank-arm.nds"
          cp -r configs "out/H-control-flow-vblank-arm/_gba/"
          cp docs/kh-save-select-control-flow-test.md "out/H-control-flow-vblank-arm/HARDWARE-TEST.md"
          cp tools/diagnostics/decode_g3cf.py "out/H-control-flow-vblank-arm/decode_g3cf.py"
      - name: Publish diagnostic artifact
        uses: actions/upload-artifact@v4
        with:
          name: GBARunner3-KH-H-control-flow-vblank-arm
          path: out/H-control-flow-vblank-arm
